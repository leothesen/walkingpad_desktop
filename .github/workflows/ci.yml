name: CI Build

on:
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4

      - name: Select Xcode
        run: sudo xcode-select -s /Applications/Xcode_16.3.app

      - name: Cache SPM packages
        uses: actions/cache@v4
        with:
          path: |
            ~/Library/Developer/Xcode/DerivedData/**/SourcePackages
            ~/Library/Caches/org.swift.swiftpm
          key: spm-${{ hashFiles('walkingpad-client.xcodeproj/project.pbxproj') }}
          restore-keys: spm-

      - name: Resolve packages
        run: xcodebuild -resolvePackageDependencies -scheme walkingpad-client

      - name: Build (Release)
        run: |
          xcodebuild -scheme walkingpad-client \
            -configuration Release \
            -destination "platform=macOS,arch=arm64" \
            CODE_SIGN_IDENTITY="-" \
            2>&1 | tee /tmp/build.log | tail -5
          echo '```' >> $GITHUB_STEP_SUMMARY
          grep -i "error:" /tmp/build.log >> $GITHUB_STEP_SUMMARY || echo "no error lines found" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          grep -q "BUILD SUCCEEDED" /tmp/build.log
